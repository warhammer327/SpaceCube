using UnityEngine;

public class Credits : MonoBehaviour
{
    // Start is called before the first frame update
    public void Quit()
    {
        Debug.Log("quite");
        Application.Quit();
    }
}
